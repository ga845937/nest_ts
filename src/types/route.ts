export enum UserRoute {
    Base = "/user",
    Login = "/login",
    Google = "/googleAuth",
    Line = "/lineAuth",
    Discord = "/discordAuth",
    Github = "/githubAuth",
    FaceBook = "/faceBookAuth",
    Telegram = "/telegramAuth",
    Authorization = "/authorization",
    UserInfo = "/userInfo",
}
